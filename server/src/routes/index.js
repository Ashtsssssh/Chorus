

const uploaderRoutes = require('./uploader_routes');
const chunkRoutes = require('./chunk_routes');
const workerRoutes = require('./worker_routes');
const authRoutes = require('./auth_routes');

module.exports = {
  uploaderRoutes,
  chunkRoutes,
  workerRoutes,
  authRoutes,
};
